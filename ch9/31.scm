

;; yes, it works.

;; use a base node class implements tree.
;; internal node and left node will inherits base class.
;; base class should have left and right children.
;; it's more convinent to construct a tree
;; but cost much space.
