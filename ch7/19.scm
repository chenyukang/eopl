
;; We wrote: “If ty1 is an unknown type, then the no-occurrence invariant tells us
;; that it is not bound in the substitution.” Explain in detail why this is so.

;; if ty1 is an unknown type and it is bound in the substitution. the substitution left side will also
;; be an unknown type.
